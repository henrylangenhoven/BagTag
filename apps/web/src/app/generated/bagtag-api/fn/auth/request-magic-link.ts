/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MagicLinkRequest } from '../../models/magic-link-request';
import { MagicLinkRequestedResponse } from '../../models/magic-link-requested-response';

export interface RequestMagicLink$Params {
  body: MagicLinkRequest;
}

export function requestMagicLink(
  http: HttpClient,
  rootUrl: string,
  params: RequestMagicLink$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<MagicLinkRequestedResponse>> {
  const rb = new RequestBuilder(rootUrl, requestMagicLink.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(rb.build({ responseType: 'json', accept: 'application/json', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MagicLinkRequestedResponse>;
    }),
  );
}

requestMagicLink.PATH = '/api/auth/magic-link/request';
