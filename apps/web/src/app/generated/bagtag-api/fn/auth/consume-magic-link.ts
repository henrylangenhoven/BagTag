/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MagicLinkConsumeRequest } from '../../models/magic-link-consume-request';
import { SessionResponse } from '../../models/session-response';

export interface ConsumeMagicLink$Params {
  body: MagicLinkConsumeRequest;
}

export function consumeMagicLink(
  http: HttpClient,
  rootUrl: string,
  params: ConsumeMagicLink$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<SessionResponse>> {
  const rb = new RequestBuilder(rootUrl, consumeMagicLink.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(rb.build({ responseType: 'json', accept: 'application/json', context })).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SessionResponse>;
    }),
  );
}

consumeMagicLink.PATH = '/api/auth/magic-link/consume';
